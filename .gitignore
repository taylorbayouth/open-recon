node_modules/
out.json
*.log
.DS_Store

# Per-run agent logs
logs/

# Per-run scratchpad output — may contain scraped page content; never commit it
runs/

# Secrets — provider API keys live here
.env
.env.*
!.env.example

# Built native binary — per-platform, rebuild via native/macos/recon-input/build.sh
native/macos/recon-input/bin/

# Release artifacts (signed/notarized dmg + zip) — produced by release.sh
native/macos/recon-input/dist/
